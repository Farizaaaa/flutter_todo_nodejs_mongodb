/**
 * !import the userdetails model
 *   const userDetails = require('./models/user.model')
 * !run server
 *  o/p:MongoDB connected
 * *check the mongodb compass we can see that inside localhost there created a database "TODo" and model "users"
 * 
 */