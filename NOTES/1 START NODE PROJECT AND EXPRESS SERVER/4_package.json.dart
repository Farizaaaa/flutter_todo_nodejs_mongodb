/**
 * activate nodemon
 * =============
 * inside "scripts"
 * !add nodemon and specify the file we wan to run initially
 *    "scripts" : {
 *     "dev" : "nodemon index"
 * 
 * }
 * !run command:npm run dev
 */