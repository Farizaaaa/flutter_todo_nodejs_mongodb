/**
 * Flutter:FRONT END
 * ----------------
 * 
 * NODE+MONGODB :BACKEND
 *   1:Routes:these are urls
 *   2:Controllers:for getting data and storing it
 *   3:services:for talking with the model and returning back the data from db,thus they communicate with the database
 *   4:models:act as databases
 * --------------------
 *   
 */